# Copy to .env and fill in locally. Never commit .env with real secrets.
# Landschaft uses Ollama Cloud remote models only (https://ollama.com) — not local Ollama.
OLLAMA_API_KEY=your_ollama_cloud_api_key_here
